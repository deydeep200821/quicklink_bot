services:
  - type: web
    name: quicklink-bot
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libzbar0
      pip install -r requirements.txt
    startCommand: python bot.py
